<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="150dp"
    android:layout_width="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/corner_radius_cardview"
    app:cardElevation="5dp"
    android:layout_margin="@dimen/padding_title_list">
    <LinearLayout     android:layout_width="100dp"
        android:padding="@dimen/padding_list"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <ImageView
            android:id="@+id/productionImg"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:src="@drawable/production"/>
        <TextView
            android:id="@+id/productionName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorBlack"
            android:layout_gravity="center"
            android:textSize="@dimen/txt_size_title"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="@string/item_production_name" />
    </LinearLayout>    
</com.google.android.material.card.MaterialCardView>
